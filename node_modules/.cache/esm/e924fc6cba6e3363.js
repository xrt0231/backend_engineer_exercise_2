let minimist,callATS,help,version;_ba6‍.x([["cli",()=>cli]]);_ba6‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_ba6‍.w("./top",[["callATS",["callATS"],function(v){callATS=v}]]);_ba6‍.w("./help",[["help",["help"],function(v){help=v}]]);_ba6‍.w("./version",[["version",["version"],function(v){version=v}]]);

//import { country } from './country';

//import { configure } from './configure';


       async function cli(argsArray) {
  const args = minimist(argsArray.slice(2));
  let cmd = args._[0] || 'help';

  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;

    case 'top':
      callATS(args);
      break;

    // case 'country':
    //   country(args);
    //   break;

    // case 'config':
    //   configure(args);
    //   break;

    default:
      console.error(`"${cmd}" is not a valid command!`);
      break;
  }
}